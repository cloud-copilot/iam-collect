{
  "name": "scan-account-function",
  "version": "1.0.0",
  "description": "Lambda function to scan AWS account resources",
  "main": "index.js",
  "scripts": {
    "build": "cp index.js ../dist/",
    "copy-modules": "rm -rf ../dist/node_modules && cp -r node_modules ../dist/",
    "install-and-build": "npm ci && npm run build && npm run copy-modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.925.0",
    "@cloud-copilot/iam-collect": "^0.1.148"
  }
}
